package main

const (
	productName = "Sigma Operator Stack"
	statusText  = "SOS is installed"
	versionText = "Version 0.1.0a4"
	instruction = "Install SOS in my current project. Show me the preview before changing it."
)

var candidate = "unbound"
