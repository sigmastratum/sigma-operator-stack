A fresh Codex session receives only the public GitHub link and one instruction: install SOS, but show the preview before changing the project.

Codex finds the canonical release pointer, verifies the exact tag, index, Linux archive, inner manifest, wheel, and checksums. The released launcher prepares its own Python and dependencies. The user does not repair PATH or install tooling.

SOS presents one aggregate plan. It will create its local control-plane records and connect the project to Codex. Qualification is excluded. Nothing in the project changes until the human confirms that exact digest.

After confirmation, installation completes and the existing user file remains byte-for-byte unchanged.

Now a genuinely fresh Codex session uses only SOS's read-only project tools. It recovers accepted authority, confirms that the workspace is current, and reports the truth: current work is not configured, qualification is not verified, and external actions still require the owner.

One public link. One visible preview. Human authority stays human. This is Sigma Operator Stack.
